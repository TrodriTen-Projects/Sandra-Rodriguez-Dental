/**
 * Content barrel — the single import surface for everything the site says.
 *
 *   import { CONTACT, BRAND, faqData, getAllPosts } from '@/content';
 */
export * from './site';
export * from './faqs';
export * from './blog';
